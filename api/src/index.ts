// Cloudflare Worker API (Hono) — placeholder, scaffolded properly in a later
// phase via create-cloudflare (see BUILD_PROMPTS.md).
export default {
  async fetch(): Promise<Response> {
    return new Response("not implemented", { status: 501 });
  },
};
